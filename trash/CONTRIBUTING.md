# Руководство по контрибьютингу

Спасибо за интерес к Git Tracker! Мы приветствуем контрибьюции сообщества.

## Как начать

### 1. Форкните репозиторий
```bash
git clone https://github.com/YOUR_USERNAME/Project_telegram_bot.git
cd Project_telegram_bot
```

### 2. Создайте feature branch
```bash
git checkout -b feature/my-feature
# или
git checkout -b bugfix/my-bugfix
```

### 3. Сделайте изменения
```bash
# Отредактируйте файлы
# Сделайте коммиты
git add .
git commit -m "feat: описание изменения"
```

### 4. Пушьте в репозиторий
```bash
git push origin feature/my-feature
```

### 5. Создайте Pull Request

## Стандарты кода

### Python (PEP 8)

```python
# ✅ Хорошо
def calculate_total(items: list[float]) -> float:
    """Calculate total sum of items.
    
    Args:
        items: List of floats to sum
        
    Returns:
        Total sum
    """
    return sum(items)

# ❌ Плохо
def calc(a):
    return sum(a)
```

**Правила:**
- Используйте 4 пробела для отступата
- Максимум 79 символов на строку
- Добавьте type hints
- Пишите docstrings для функций

### Go (gofmt)

```go
// ✅ Хорошо
func (d *WebhookDispatcher) Handle(eventType string, payload interface{}) error {
    handler, exists := d.handlers[eventType]
    if !exists {
        return fmt.Errorf("no handler found for event type: %s", eventType)
    }
    return handler(payload)
}

// ❌ Плохо
func (d *WebhookDispatcher) Handle(et string, p interface{}) error {
if h, ok := d.handlers[et]; ok {
return h(p)
}
return nil
}
```

**Правила:**
- Используйте `gofmt` для форматирования
- Называйте переменные ясно
- Добавьте комментарии для публичных функций
- Используйте `errors.Is()` и `errors.As()` для обработки ошибок

## Коммит сообщения

Используйте Conventional Commits:

```
feat: добавить новую функцию
fix: исправить баг в обработчике
docs: обновить README
style: отформатировать код
refactor: переделать логику
perf: улучшить производительность
test: добавить тесты
chore: обновить зависимости
```

**Примеры:**
```
feat: добавить поддержку Telegram форумов
fix: исправить ошибку при подключении к MongoDB
docs: добавить примеры конфигурации
```

## Тестирование

### Python тесты

```bash
cd bot

# Запустите тесты
python -m pytest tests/ -v

# С покрытием
python -m pytest tests/ --cov=. --cov-report=html
```

### Go тесты

```bash
cd webhook_server

# Запустите тесты
go test ./...

# С покрытием
go test -cover ./...
go tool cover -html=coverage.out
```

## Документация

При добавлении нового функционала:

1. **Обновите README.md** - если это основная функция
2. **Добавьте комментарии в коде** - для всех публичных функций
3. **Обновите ARCHITECTURE.md** - если изменилась архитектура
4. **Создайте миграцию для БД** - если нужны изменения в схеме

### Пример docstring (Python)

```python
def create_webhook(name: str, user_id: int) -> str:
    """
    Create a new GitHub webhook.
    
    Creates a new webhook with random URL and saves it to database.
    
    Args:
        name: Webhook display name
        user_id: ID of the user creating the webhook
        
    Returns:
        Generated webhook URL
        
    Raises:
        ValueError: If name is empty
        pymongo.errors.DuplicateKeyError: If webhook name exists
        
    Example:
        >>> url = create_webhook("My Webhook", 12345)
        >>> print(url)
        http://localhost:8080/github-webhook/abc123def456
    """
```

## Pull Request Процесс

### Checklist перед тем как создать PR

- [ ] Код следует стандартам проекта
- [ ] Добавлены/обновлены тесты
- [ ] Документация обновлена
- [ ] Нет конфликтов с `main` веткой
- [ ] Коммиты имеют понятные сообщения
- [ ] Наготовлены по инструкциям (фиксеры, стили и т.д.)

### PR Template

```markdown
## Описание

Коротко опишите ваши изменения.

## Тип изменения

- [ ] Bug fix
- [ ] New feature
- [ ] Breaking change
- [ ] Documentation update

## Как это тестировалось?

Опишите шаги для тестирования.

## Скриншоты (если применимо)

## Issues

Closes #123
```

## Таговое соглашение (для релизов)

```
v1.0.0 - Major.Minor.Patch

- Major: Breaking changes
- Minor: New features (backward compatible)
- Patch: Bug fixes
```

## Баги и Issues

### Как создать хороший issue

```markdown
## Описание проблемы

Четко опишите, в чем проблема.

## Как воспроизвести

1. День 1
2. День 2
3. День 3

## Ожидаемое поведение

Что должно было случиться?

## Фактическое поведение

Что случилось на самом деле?

## Окружение

- OS: Windows 11 / Linux / macOS
- Python version: 3.11
- Go version: 1.21
- Docker version: 24.0

## Логи

\`\`\`
[ERROR] Stack trace or error message
\`\`\`
```

## Кодекс поведения

- Будьте вежливы и уважительны
- Не спамьте
- Не используйте оскорбительный язык
- Помогайте новичкам
- Конструктивная критика приветствуется

## Лицензия

Все контрибьюции открыты под MIT License.

## Вопросы?

- Создайте GitHub Discussion
- Откройте GitHub Issue
- Посетите наш Discord сервер (если есть)

---

**Спасибо за контрибьюцию!** 🙏
